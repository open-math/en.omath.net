const quadraticFormulas = $CONTENT.foundations.equations.quadratic.formulas;

export default (
  <>
    <P>
      All the formulas and solution methods for quadratic equations, along with mixed problems that reduce to them.{' '}
      <Ref to={quadraticFormulas}>The new topic</Ref> is already live!
    </P>
  </>
);
