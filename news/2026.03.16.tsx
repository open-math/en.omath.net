const zeroProductProperty = $CONTENT.foundations.equations.zeroProductProperty;

export default (
  <>
    <P>
      New page about the <Ref to={zeroProductProperty}>zero product method</Ref>, which lets you quickly solve any
      equations made up of factors whose product equals zero.
    </P>
  </>
);
